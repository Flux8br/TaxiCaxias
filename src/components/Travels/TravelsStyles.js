import { StyleSheet } from 'react-native';

export default TravelsStyles = StyleSheet.create({
    container: {
        flex: 1,
    },

    innerContainer: {
        flex: 1,
        marginTop: 80,
        padding: 10
    },
    card: {
        backgroundColor: '#ededed',
        marginBottom: 10,
        padding: 8,
    },
})