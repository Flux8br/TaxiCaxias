import { StyleSheet } from 'react-native';

export default AddressStyle = StyleSheet.create({
    container: {
        flex: 1
    },

    innerContainer: {
        marginTop: 80,
        padding: 10
    },
    row: {
        backgroundColor: '#ededed',
        marginBottom: 5
    },
    addButton: {
        position: 'absolute',
        bottom: 0,
        width: '100%'
    },
})