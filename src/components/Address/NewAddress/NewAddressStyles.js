import { StyleSheet } from 'react-native';

export default NewAddressStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },
})