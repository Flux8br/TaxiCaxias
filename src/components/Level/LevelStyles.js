import { StyleSheet } from 'react-native';

export default LevelStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    innerContainer: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },
    textContainer: {
        width: '50%',
        textAlign: 'center',
        fontSize: 18,
        marginTop: 30
    }
})